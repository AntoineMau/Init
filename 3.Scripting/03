#!/bin/bash
cd ..
git status
read -p "Entre les fichiers que tu veux add: " FILES
git add $FILES
echo "$FILES"
read -p "Entre le message que tu veux commit: " MSG
git commit -m "$MSG"
git push
echo "push termine:\n"
git status
cd 3.Scripting
