#!/bin/bash
PATH_OLD=`pwd`
cd ..
sudo git status
read -p "Entre les fichiers que tu veux add: " FILES
sudo git add $FILES
echo "$FILES"
read -p "Entre le message que tu veux commit: " MSG
git commit -m "$MSG"
git push
echo "push termine:\n"
git status
cd $PATH_OLD
